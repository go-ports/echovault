{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.go-ports/echovault",
  "description": "Local-first memory for coding agents. Persists decisions, bugs, and context across sessions as Markdown — no cloud, no API keys, no cost.",
  "title": "EchoVault",
  "repository": {
    "url": "https://github.com/go-ports/echovault",
    "source": "github"
  },
  "version": "${VERSION}",
  "packages": [
    {
      "registryType": "oci",
      "identifier": "ghcr.io/go-ports/echovault:${VERSION}",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "MEMORY_HOME",
          "description": "Directory where memories are stored. Mount a host path to this location for persistence across container restarts.",
          "isRequired": false,
          "format": "filepath",
          "default": "/root/.memory"
        }
      ]
    }
  ]
}
